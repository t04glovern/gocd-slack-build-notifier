gocd.spark {
  login = "foo
  password = "foo-bar"
  server-host = "https://go-instance:8153/"
  webhookUrl = "https://api.ciscospark.com/"

  pipelines = [{
    name = ".*"
    stage = ".*"
    state = "failed"
    room = "#foo"
  }]
}
